global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "phosprocess-api"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "api:8000"

  - job_name: "nvidia-gpu"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "nvidia_gpu_exporter:9835"
